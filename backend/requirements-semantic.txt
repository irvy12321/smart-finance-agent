# Optional dependencies for SemanticEmbedder (app/rag/embed.py) and the RAG
# evaluation harness (scripts/rag_eval.py). NOT required for the core app or CI:
# the lexical dev/prod embedders work without these, and SemanticEmbedder raises
# a clear ImportError if neither backend is installed.
#
#   pip install -r requirements-semantic.txt
#
# Production backend for the pinned Chinese BGE model. This pulls in CPU PyTorch;
# keep it separate from requirements.txt so ordinary CI never downloads it.
sentence-transformers>=5.0,<6

# Optional alternative for experiments only. The default potion model is not
# the Chinese production model and must not be reported as equivalent to BGE.
# model2vec>=0.3.0
