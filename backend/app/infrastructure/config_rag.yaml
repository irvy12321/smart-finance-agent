# RAG 基础配置
chunk_size: 500
chunk_overlap: 50
embedding_dim: 384
top_k: 5

# Embedding 配置
# mode: "prod" = hash mock (快速, 无语义), "prod" = bge-m3 (真实语义)
embedding:
  mode: "prod"
  model_name: "all-MiniLM-L6-v2"
  dim: 1024
  batch_size: 32
  device: "cpu"
